# KataRank — pinned runtime dependencies (core + API server)
#
# Source install (git clone):
#   pip install -e .               # install katarank itself (editable)
#   pip install -r requirements.txt  # pin transitive deps to these versions
#
# Wheel install (from GitHub Releases):
#   pip install 'katarank[api] @ katarank-*.whl'
#   # All deps are declared in the wheel; use requirements-api.txt for pinning.
#
# Generated by: uv export --extra api --no-hashes --no-dev --no-emit-project
annotated-doc>=0.0.4
annotated-types>=0.7.0
anyio>=4.0
click>=8.0
fastapi>=0.110
filelock>=3.0
h11>=0.14
httptools>=0.6
jinja2>=3.0
numpy>=1.24
pydantic>=2.0
pydantic-core>=2.0
python-dotenv>=1.0
pyyaml>=6.0
starlette>=1.0
torch>=2.12.0
typing-extensions>=4.0
uvicorn[standard]>=0.29
watchfiles>=1.0
websockets>=13.0
